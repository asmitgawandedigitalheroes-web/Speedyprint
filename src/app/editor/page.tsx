'use client'

import EditorShell from '@/components/editor/EditorShell'

/**
 * /editor
 *
 * Standalone design editor without a specific product template.
 * Uses default 800×600 canvas. Useful for free-form design.
 */
export default function EditorPage() {
  return <EditorShell />
}
